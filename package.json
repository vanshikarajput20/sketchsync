{
  "name": "sketchsync",
  "version": "1.0.0",
  "description": "SketchSync — real-time collaborative whiteboard monorepo",
  "private": true,
  "scripts": {
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "install:all": "cd server && npm install && cd ../client && npm install && cd ../tests && npm install",
    "test": "cd tests && npm test"
  }
}
